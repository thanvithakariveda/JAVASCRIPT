// Function to calculate the sum of two array elements

function arraySum([a, b]) {

    // Add both values
    let sum = a + b;

    // Return the result
    return sum;
}

// Calling the function with array values
let result = arraySum([30, 60]);

// Print the output
console.log("Sum of array elements is:", result);